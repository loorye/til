# ====================
# AWS Parameter Store の設定
# ====================
# 本番環境では、以下の全ての設定値を AWS Parameter Store から自動取得します
# ローカル開発時のみ、この .env ファイルに環境変数を設定してください
#
# セットアップスクリプト: ./scripts/setup-parameter-store.sh
#
# Parameter Store のパス一覧:
#   機密情報（SecureString）:
#     - /ai-workshop/openai-api-key
#     - /ai-workshop/google-api-key
#     - /ai-workshop/bedrock-access-key-id
#     - /ai-workshop/bedrock-secret-access-key
#     - /ai-workshop/bedrock-session-token
#     - /ai-workshop/auth-username
#     - /ai-workshop/auth-password
#   設定値（String）:
#     - /ai-workshop/openai-model
#     - /ai-workshop/gemini-model
#     - /ai-workshop/bedrock-model-id
#     - /ai-workshop/bedrock-region
#     - /ai-workshop/mock-mode
#     - /ai-workshop/disable-auth

# ====================
# 動作モード
# ====================
# Parameter Store: /ai-workshop/mock-mode
# true の場合は外部APIを呼ばずに擬似レスポンスを返す
MOCK_MODE=true
NEXT_PUBLIC_MOCK_MODE=true

# ====================
# Basic 認証設定
# ====================
# Parameter Store:
#   - /ai-workshop/auth-username (SecureString)
#   - /ai-workshop/auth-password (SecureString)
#   - /ai-workshop/disable-auth (String)
# 本番環境では必ず強力なパスワードを設定してください
AUTH_USERNAME=admin
AUTH_PASSWORD=your-secure-password
# DISABLE_AUTH=true # 認証を無効化する場合（ローカル開発時など）

# ====================
# AI モデル API 認証情報
# ====================
# Parameter Store:
#   - /ai-workshop/openai-api-key (SecureString)
#   - /ai-workshop/google-api-key (SecureString)
#   - /ai-workshop/bedrock-access-key-id (SecureString)
#   - /ai-workshop/bedrock-secret-access-key (SecureString)
#   - /ai-workshop/bedrock-session-token (SecureString, オプション)
# MOCK_MODE=false の場合に設定
OPENAI_API_KEY=
GOOGLE_API_KEY=
# 注: AWS_プレフィックスは予約されているため、BEDROCK_プレフィックスを使用
BEDROCK_ACCESS_KEY_ID=
BEDROCK_SECRET_ACCESS_KEY=
BEDROCK_SESSION_TOKEN= # optional (assume roleの場合)

# ====================
# モデル設定
# ====================
# Parameter Store:
#   - /ai-workshop/openai-model (String)
#   - /ai-workshop/gemini-model (String)
#   - /ai-workshop/bedrock-model-id (String)
#   - /ai-workshop/bedrock-region (String)
# 使用する AI モデルの指定（デフォルト値）
OPENAI_MODEL=gpt-4o-mini
BEDROCK_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20240620-v1:0
GEMINI_MODEL=gemini-1.0-pro

# ====================
# AWS 設定
# ====================
# AWS Parameter Store のリージョン
AWS_REGION=us-east-1
